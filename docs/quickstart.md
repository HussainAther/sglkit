# Quickstart

Install with `pip install -e ".[dev]"`, then run the examples in numerical order.

The package separates reusable scientific components from paper-specific experiment orchestration. For published work, record exact package version, seed ranges, detector configuration, filter configuration, and forward-model assumptions.
