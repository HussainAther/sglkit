# Reconstruction

Two reconstruction levels are provided:

1. nearest-raster accumulation as a transparent baseline;
2. forward-model-aware TV-regularized optimization using the same simplified scalar SGL forward operator.

The iterative solver is intended for method development, not as a claim of optimal SGL inversion. Additional priors, sparse representations, uncertainty quantification, and published reconstruction baselines are natural extensions.
