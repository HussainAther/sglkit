# Spectral filtering

`biomimetic_filter` is a phenomenological carotenoprotein-inspired response with absorption features near 384, 405, 433, and 458 nm plus a tunable cutoff.

It is a design-space model, not a measured flight filter. Report planet throughput and background throughput separately; do not assume a filter is beneficial without evaluating the target and background spectra together.
